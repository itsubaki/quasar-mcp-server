{
    "mcpServers": {
        "quasar-mcp-server": {
            "command": "${YOUR_GO_PATH}/go/bin/quasar-mcp-server",
            "args": [],
            "env": {
                "BASE_URL": "${YOUR_BASE_URL}",
                "IDENTITY_TOKEN": "${YOUR_IDENTITY_TOKEN}"
            }
        }
    }
}
